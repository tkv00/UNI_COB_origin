<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 테두리 색상을 회색으로 설정 -->
    <stroke
        android:width="2dp"
        android:color="@color/Gray_03"/>

    <!-- 배경 색상을 흰색으로 설정 -->
    <solid android:color="@color/Gray_03" />

    <!-- 모서리를 둥글게 설정 -->
    <corners android:radius="10dp" />
</shape>